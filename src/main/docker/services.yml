# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: prayerrosterbackend
services:
  postgresql:
    extends:
      file: ./postgresql.yml
      service: postgresql
  # No Keycloak: Google is the only identity provider (stateless Resource Server, see
  # SecurityConfiguration) - nothing here plays the role JHipster's default Keycloak service does.
